export default function EditTemplate({ params }: { params: Promise<{ id: string }> }) {
  return (
    <div>
      <h1 className="mb-6 text-2xl font-bold">Edit Template</h1>
      <p className="text-gray-500">Template editor coming in Step 2.</p>
    </div>
  );
}
